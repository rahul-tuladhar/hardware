models:
   image: tp33/django
   external_links:
      - mysql:db
   ports:
      - "8001:8000"
   volumes:
      - ./hardware_models:/app
   command: bash -c "pip install requests && python manage.py makemigrations posts && python manage.py migrate && python manage.py loaddata fixtures.json && mod_wsgi-express start-server --reload-on-changes hardware_models/wsgi.py"
#  python manage.py loaddata fixtures.json &&
exp:
   image: tp33/django
   links:
      - models:models-api
   ports:
      - "8002:8000"
   volumes:
      - ./hardware_exp:/app
   command: bash -c "pip install requests && mod_wsgi-express start-server --reload-on-changes hardware_exp/wsgi.py"

web:
   image: tp33/django
   links:
      - exp:exp-api
   ports:
      - "8000:8000"
   volumes:
      - ./hardware_web:/app
   command: bash -c "pip install requests && mod_wsgi-express start-server --reload-on-changes hardware_web/wsgi.py"
